require 'rails_helper'

describe "after user submits text and clicks save" do
  it "the data is persisted in database" do
    # need to add title after james PR merged

    # this is going to be in integration test with selenium when we get it working
  end
end
